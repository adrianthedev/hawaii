<div class="flex flex-col">
  <%= render Avo::PanelComponent.new(name: "Comments resource tool") do |c| %>
    <% c.tools do %>
      <%= a_link('/avo', icon: 'heroicons/solid/academic-cap', color: :primary, style: :primary) do %>
        Dummy link
      <% end %>
    <% end %>

    <% c.body do %>
      <div class="flex flex-col p-4 min-h-24">
        <div class="space-y-4">
          <h3>🪧 This partial is waiting to be updated</h3>
current account: <%= Current.account %>

          <div data-controller="visuality">
            <input type="text"
            data-visuality-target="input"
            data-action="visuality#onInput"
            >
          </div>

          <%= turbo_frame_tag :something do %>
            <%= params[:something_else] %>
          <% end %>

          <%
            # In this partial you have access to the following variables:
            # tool
            # @resource
            # @resource.model
            # params
            # Avo::App.context
            # current_user
          %>
        </div>
      </div>
    <% end %>
  <% end %>
</div>


